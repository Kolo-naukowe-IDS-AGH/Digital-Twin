version: '3'
services:
#  frontend:
#    build:
#      context: ./frotnend
#    command: npm run start
#    container_name: frontend
#    ports:
#      - "3000:3000"
#    volumes:
#      - ./:/frontend
#      - /frontend/node_modules
  frontend:
    stdin_open: true
    build:
      dockerfile: Dockerfile
      context: ./client
    volumes:
      - /app/node_modules
      - ./client:/app
    ports:
      - '3000:3000'
    command: npm start